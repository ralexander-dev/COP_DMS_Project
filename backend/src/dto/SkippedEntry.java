package dto;

/**
 * DTO for skipped entries during import, containing the entry that was skipped and the reason for skipping.
 * @author Russell Alexander
*/
public class SkippedEntry {
  public String entry;
  public String reason;
}
